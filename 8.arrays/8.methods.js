/*
 ? push: insert elements at end
 ? unshift: insert elements at front
 ? pop: remove elements from the end
 ? shif: remove elements from the front
*/ 
var array = [1,2,3,4]
array.push(5)
array.push(6)
array.push(7)

array.unshift(0)
array.unshift(-1)
array.unshift(-2)

array.pop()

array.shift()

console.log(array)